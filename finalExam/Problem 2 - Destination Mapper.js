function solve(input){
let regex = /(=|\/)([A-Z][-Za-z]{3,})\1/gm
let matches = input.match(regex)
let totalPoints =0
for(let line of matches){
    let length = Number(line.length -2 )
    totz
}
}
solve("=Hawai=/Cyprus/=Invalid/invalid==i5valid=/I5valid/=i=")